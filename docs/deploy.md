# Deploy

- aponte o servidor para `public/`
- copie `.env.example` para `.env`
- desabilite debug em produção
- garanta permissão de escrita em `storage/`
- configure HTTPS para ativar cookies `secure`
