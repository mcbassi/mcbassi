# V14.4.1 — Execução SQL na tela de edição de prompts

- botão `Exec. SQL` executa a sentença no mesmo fluxo do editor
- marcadores do SQL são resolvidos com a versão vigente do questionário
- apenas `SELECT/WITH` são permitidos
- resultado:
  - JSON formatado
  - tabela HTML
  - texto formatado
