# V5

## A
- `index.php` virou shell inicial real
- login por query string compatível com o legado
- redirect limpo após autenticação
- exibição do usuário autenticado no shell

## B
- `tela_sql` funcional
- execução de `SELECT` e `WITH`
- catálogo salvo em `storage/app/tela_sql_catalog.json`
- inspeção de schema
- base path automático para subpasta
