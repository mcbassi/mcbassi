# V14.4.7 — SQL visível em Original e Prompt pronto

## Ajuste
- a aba **Original** volta a mostrar o prompt completo, incluindo o bloco `EXECUTAR SQL=`
- a aba **Prompt pronto** também passa a mostrar o bloco SQL ao final
- a caixa lateral de SQL continua existindo
- mudanças na lateral de SQL passam a refletir nas abas principais
